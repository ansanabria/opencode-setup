# AGENTS.md

This repository is the user's global OpenCode configuration directory at `~/.config/opencode`.

Its purpose is to define the user's default OpenCode environment, including global runtime settings, TUI settings, agents, plugins, themes, commands, rules, skills, and supporting files used across OpenCode sessions.
